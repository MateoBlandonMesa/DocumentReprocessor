# Ignore build artifacts and user-specific files
bin/
obj/
publish/
*.user
*.suo
.vs/

# Local configuration with secrets (token, machine paths). Use appsettings.Example.json as template.
appsettings.json
appsettings.Development.json
